exe = bin/luna.exe
sources = src/*.cpp external/libraries/*
includes = /I include /I external/include
res = build/windows/resource.res

common_flags = /std:c++latest /EHsc /W2 /MP
release_flags = /Ox

debug:
	cl $(sources) /Fe$(exe) $(includes) $(common_flags) /D LUNA_DEBUG /D LUNA_STANDALONE
	del *.obj

install: resource
	cl $(sources) $(res) /Fe$(exe) $(includes) $(common_flags) $(release_flags)
	del *.obj

standalone: resource
	cl $(sources) $(res) /Fe$(exe) $(includes) $(common_flags) $(release_flags) /D LUNA_STANDALONE
	del *.obj

out-install: clean install
	md out\res
	xcopy /e /y bin out
	xcopy /e /y res out\res\*

out-standalone: clean standalone
	md out\res
	xcopy /e /y bin out
	xcopy /e /y res out\res\*

clean:
	if exist bin\Luna.exe del bin\Luna.exe
	if exist out rd /s /q out

resource:
	rc build/windows/resource.rc